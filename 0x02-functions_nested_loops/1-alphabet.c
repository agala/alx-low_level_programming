#include <stdio.h>
#include <main_h>

/**
 * main - progarm that prints alphabets in lowercase followed by a new line
 *
 * Return: 0 (Success)
*/
int main(void)
{

	char alph = "abcdefghijklmnopqrstuvwxyz";

	putchar("\n", alph);
	return (0);
}
